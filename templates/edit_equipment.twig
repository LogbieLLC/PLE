<!DOCTYPE html>
<html>
<head>
    <title>PLE System - Edit Equipment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
</head>
<body>
    <div class="container mt-4">
        <header class="mb-4">
            <h1 class="mb-3">Edit Equipment - {{ equipment.ple_id }}</h1>
            <nav>
                <a href="index.php" class="btn btn-outline-secondary">Back to List</a>
            </nav>
        </header>

        <main>
            <form method="POST" action="index.php?action=editEquipment&id={{ equipment.id }}" class="col-md-8 mx-auto">
                <div class="mb-3">
                    <label for="type" class="form-label">Equipment Type:</label>
                    <input type="text" value="{{ equipment.type }}" class="form-control-plaintext" readonly disabled>
                    <div class="form-text">Type and PLE ID cannot be changed</div>
                </div>

                <div class="mb-3">
                    <label for="ple_id" class="form-label">PLE ID:</label>
                    <input type="text" value="{{ equipment.ple_id }}" class="form-control-plaintext" readonly disabled>
                </div>

                <div class="mb-3">
                    <label for="make" class="form-label">Make:</label>
                    <input type="text" name="make" id="make" class="form-control" value="{{ equipment.make }}" required>
                </div>

                <div class="mb-3">
                    <label for="model" class="form-label">Model:</label>
                    <input type="text" name="model" id="model" class="form-control" value="{{ equipment.model }}" required>
                </div>

                <div class="mb-3">
                    <label for="serial_number" class="form-label">Serial Number:</label>
                    <input type="text" name="serial_number" id="serial_number" class="form-control" value="{{ equipment.serial_number }}" required>
                </div>

                <div class="mb-3">
                    <label for="department" class="form-label">Department:</label>
                    <select name="department" id="department" class="form-select" required>
                        <option value="Big Backroom" {% if equipment.department == 'Big Backroom' %}selected{% endif %}>Big Backroom</option>
                        <option value="Garden Center" {% if equipment.department == 'Garden Center' %}selected{% endif %}>Garden Center</option>
                    </select>
                </div>

                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Update Equipment</button>
                </div>
            </form>
        </main>
    </div>
    {% include '_time.twig' %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
</body>
</html>
