<!DOCTYPE html>
<html>
<head>
    <title>PLE System - Settings</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
</head>
<body>
    <div class="container mt-4">
        <header class="mb-4">
            <h1 class="mb-3">System Settings</h1>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="container-fluid">
                    <div class="navbar-nav me-auto">
                        <a href="index.php" class="btn btn-outline-secondary">Back to Home</a>
                    </div>
                    <div class="navbar-text">
                        Logged in as: {{ user.username }}
                        <a href="index.php?action=logout" class="btn btn-outline-secondary ms-2">Logout</a>
                    </div>
                </div>
            </nav>
        </header>

        <main>
            {% if not isAdmin %}
                <div class="alert alert-danger">
                    <p class="mb-0">Access denied. Admin privileges required.</p>
                </div>
            {% else %}
                <div class="card">
                    <div class="card-body">
                        <form method="POST" action="index.php?action=settings">
                            <div class="form-check mb-3">
                                <input class="form-check-input" type="checkbox" name="debug_mode" id="debug_mode" {% if debugMode %}checked{% endif %}>
                                <label class="form-check-label" for="debug_mode">Enable Debug Mode</label>
                                <small class="text-muted d-block mt-1">
                                    When enabled, inspection time restrictions (12:00 AM - 7:00 AM) will be bypassed.
                                    Use only for testing purposes.
                                </small>
                            </div>

                            <button type="submit" class="btn btn-primary">Save Settings</button>
                        </form>
                    </div>
                </div>
            {% endif %}
        </main>

        {% include '_time.twig' %}
    </div>
</body>
</html>
